{
  "name": "cloudbox",
  "private": true,
  "workspaces": ["apps/server", "apps/web"],
  "scripts": {
    "dev": "pnpm --filter cloudbox-server dev & pnpm --filter cloudbox-web dev",
    "build": "pnpm -r run build",
    "start": "pnpm --filter cloudbox-server start"
  },
  "packageManager": "pnpm@9.12.0"
}
